# 07. 人事管理・給与明細・日報

関連ファイル: `routes/hr.js`（2845行）

---

## 1. エンドポイント一覧

### 人事管理

| メソッド | パス | 権限 | 説明 |
|---------|------|------|------|
| GET | `/hr` | requireLogin | 社員一覧（管理者: 全員 / 一般: 自分） |
| GET | `/hr/add` | requireLogin | 社員追加フォーム |
| POST | `/hr/add` | requireLogin | 社員追加処理 |
| GET | `/hr/edit/:id` | requireLogin | 社員情報編集フォーム |
| POST | `/hr/edit/:id` | requireLogin | 社員情報更新 |
| GET | `/hr/delete/:id` | requireLogin | 社員削除 |
| GET | `/hr/statistics` | requireLogin | 社員統計ページ |
| POST | `/hr/leave/:id` | requireLogin | 社員の休暇ステータス更新 |
| GET | `/hr/export` | requireLogin | 社員一覧 Excel エクスポート |
| POST | `/hr/photo/:id` | requireLogin | 社員写真アップロード |

### 給与管理

| メソッド | パス | 権限 | 説明 |
|---------|------|------|------|
| GET | `/hr/payroll/admin` | requireLogin | 給与管理トップ |
| GET | `/hr/payroll/admin/new` | requireLogin | 給与明細新規作成フォーム |
| POST | `/hr/payroll/admin/add` | requireLogin | 給与明細作成 ＋**通知** |
| GET | `/hr/payroll/admin/edit/:slipId` | requireLogin | 給与明細編集フォーム |
| POST | `/hr/payroll/admin/edit/:slipId` | requireLogin | 給与明細更新 ＋**通知**（条件付き） |
| POST | `/hr/payroll/admin/delete/:slipId` | requireLogin | 給与明細削除 |
| GET | `/hr/payroll` | requireLogin | 自分の給与明細一覧（直近12件） |
| GET | `/hr/payroll/:id` | requireLogin | 社員別給与明細詳細 |
| GET | `/hr/payroll/:id/export` | requireLogin | 給与明細 PDF エクスポート |

### 日報

| メソッド | パス | 権限 | 説明 |
|---------|------|------|------|
| GET | `/hr/daily-report` | requireLogin | 日報一覧（全社員 / 自分フィルター） |
| GET | `/hr/daily-report/new` | requireLogin | 日報作成フォーム |
| POST | `/hr/daily-report/new` | requireLogin | 日報作成 |
| GET | `/hr/daily-report/:id` | requireLogin | 日報詳細 |
| GET | `/hr/daily-report/:id/edit` | requireLogin | 日報編集フォーム（作成者のみ） |
| POST | `/hr/daily-report/:id/edit` | requireLogin | 日報更新（作成者のみ） |
| POST | `/hr/daily-report/:id/delete` | requireLogin | 日報削除（作成者・管理者） |
| POST | `/hr/daily-report/:id/reaction` | requireLogin | 日報スタンプ ＋**通知** |
| POST | `/hr/daily-report/:reportId/comment/:commentId/reaction` | requireLogin | コメントスタンプ ＋**通知** |
| POST | `/hr/daily-report/:id/comment` | requireLogin | コメント投稿 ＋**通知** |

---

## 2. 人事管理

### 社員統計ページ（/hr/statistics）

| グラフ | 内容 |
|--------|------|
| 部署別人数 | 部署ごとの社員数（円グラフ） |
| 役職別人数 | 役職ごとの社員数 |
| 入社年別人数 | 年別の入社人数（棒グラフ） |

### 写真アップロード

```
POST /hr/photo/:id
  └── Multer（storage: uploads/）
        ├── ファイル名: {timestamp}-{random}{ext}
        └── Employee.photoUrl を更新
```

### Excel エクスポート（/hr/export）

- ExcelJS で全社員データを出力
- 列: 社員番号・氏名・部署・役職・入社日・電話番号・メールアドレス

---

## 3. 給与明細

### 給与明細作成フロー

```
POST /hr/payroll/admin/add
  ├── PayrollRun.create({periodFrom, periodTo, fiscalYear})
  ├── PayrollSlip.create({
  │     runId, employeeId,
  │     workDays, absentDays, lateCount, overtimeHours, nightHours, holidayHours,
  │     baseSalary, allowances[], deductions[], commute, incomeTax,
  │     gross, net, status, notes
  │   })
  └── status が 'issued' 以上 →
        createNotification({ type: 'payslip_issued', 受信者: 該当社員 })
```

### 給与明細更新フロー

```
POST /hr/payroll/admin/edit/:slipId
  ├── PayrollSlip を更新
  └── 旧 status が 'draft' かつ 新 status が 'issued' 以上 →
        createNotification({ type: 'payslip_issued', 受信者: 該当社員 })
```

### ステータス遷移

```
draft → issued → locked → paid
```

| ステータス | 意味 |
|-----------|------|
| draft | 作成中・社員未公開 |
| issued | 発行済み・社員が閲覧可能 |
| locked | 確定（編集不可） |
| paid | 支払済み |

### PDF エクスポート（/hr/payroll/:id/export）

- html-pdf を使用
- 明細レイアウト HTML を生成 → PDF 変換 → ダウンロード

---

## 4. 日報

### 日報作成フォームの入力項目

| フィールド | 必須 | 説明 |
|-----------|------|------|
| reportDate | ✅ | 日報日付 |
| content | ✅ | 本文 |
| achievements | — | 本日の成果 |
| issues | — | 課題・問題点 |
| tomorrow | — | 明日の予定 |

### スタンプリアクション

```
POST /hr/daily-report/:id/reaction
  ├── body: { emoji: 'like' | 'great' | 'nice' | 'hard' | 'check' }
  ├── 既に押していれば取り消し（トグル）
  └── 自分の日報でなければ →
        createNotification({ type: 'reaction', 受信者: 日報作成者 })
```

### コメント投稿

```
POST /hr/daily-report/:id/comment
  ├── DailyReport の comments 配列に push
  └── 自分の日報でなければ →
        createNotification({ type: 'comment', 受信者: 日報作成者 })
```

### コメントへのスタンプ

```
POST /hr/daily-report/:reportId/comment/:commentId/reaction
  ├── comment.reactions 配列でトグル
  └── 自分のコメントでなければ →
        createNotification({ type: 'reaction', 受信者: コメント投稿者 })
```
